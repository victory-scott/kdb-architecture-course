[Unit]
Description=KX Tickerplant HDB — kxtpstack
After=kx-tp.service
Requires=kx-tp.service

[Service]
Type=simple
EnvironmentFile=-%h/.config/kxtpstack/common.env
# MemoryMax=8G
# CPUQuota=150%
LimitNOFILE=131072
ExecStart=/usr/bin/env bash -lc 'exec "${QBIN:-/opt/kx/q}" "${APP_DIR}/tick/hdb.q" "${HDB_DIR:-$HOME/kx-hdb}" -p "${HDB_PORT:-5012}" ${QOPTS}'
Restart=on-failure
RestartSec=1
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=default.target
